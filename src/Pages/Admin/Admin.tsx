import { createUseStyles } from "react-jss";
import { Theme } from "../../Styling/Theme";

const useStyles = createUseStyles((theme: Theme) => {
	return {
	};
});

export const Admin: React.FC<{ dispatch: any }> = ({ dispatch }) => {
	const classes = useStyles();

	return (
		<>
		</>
	);
};
